{
  "name": "icavs-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev functions --compatibility-date=2024-01-01",
    "deploy": "wrangler pages deploy",
    "db:generate": "drizzle-kit generate:pg",
    "db:push": "drizzle-kit push:pg",
    "db:migrate": "node -r dotenv/config db/migrate.ts",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@clerk/backend": "^0.38.15",
    "@neondatabase/serverless": "^0.9.5",
    "drizzle-orm": "^0.29.5",
    "hono": "^4.8.5",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "drizzle-kit": "^0.20.0",
    "wrangler": "^3.0.0"
  }
}
